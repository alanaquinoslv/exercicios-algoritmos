function papagaio() {
    var contador = 0
    var numero = Number(ipt_num.value)
    var frase = ipt_frase.value
    
        while (contador < numero) {
            alert (`${frase}`)
            contador++
        }

    
}